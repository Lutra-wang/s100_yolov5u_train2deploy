# Board runtime output

Purpose: stores RDK S100 inference and performance results. Producing stage: run the board inference and profiling commands. Expected filenames: `bpu_inference.log`, `bpu_result.txt`, `model_info.log`, and `perf_*.log`. Success signal: inference and profiling complete with the listed result and log files. The artifacts are absent from Git because they are generated runtime outputs.
