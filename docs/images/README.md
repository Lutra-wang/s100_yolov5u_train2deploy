# Documentation screenshots

Purpose: stores screenshots used by the operation manual. Producing stage: capture evidence while completing the documented workflow. Expected filenames: `S0-01` plus the S1/S2/S3 screenshot filenames defined by the specification. Success signal: each required screenshot is present and legible in the manual. The artifacts are absent from Git because screenshots are generated evidence assets maintained outside the source-controlled workflow.
