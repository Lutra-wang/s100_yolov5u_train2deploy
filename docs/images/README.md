# Documentation images

Purpose: stores the architecture image and screenshots used by the operation manual. Producing stage: the architecture image is supplied with the initial manual; capture the remaining evidence while completing the documented workflow. Expected filenames: `S0-01-project-architecture.jpg` plus the S1/S2/S3 screenshot filenames defined by the specification. Success signal: each required image is present, legible, linked from the manual, and tracked in Git.
