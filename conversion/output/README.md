# Compilation output

Purpose: stores BPU compilation results. Producing stage: run the model conversion/compilation command. Expected filenames: `yolov5nu_s100_nv12.hbm` and `hb_compile.log`. Success signal: compilation succeeds and produces the HBM model with its compile log. The artifacts are absent from Git because they are generated deployment outputs.
